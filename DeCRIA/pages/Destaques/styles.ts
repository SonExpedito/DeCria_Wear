import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    topSearchsContainer: {
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center',
        gap: 14,
    },
    destaqueButton: {
        width: '100%',
        alignItems: 'center',
    }
    ,
    destaqueImage: {    
       width: '100%',
       height: 90
    },

})